{
  "name": "@open-transit-map/infra",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "build": "tsc -b",
    "test": "vitest run",
    "test:watch": "vitest",
    "test:coverage": "vitest run --coverage",
    "test:coverage:summary": "vitest run --coverage --coverage.reporter=text-summary --reporter=basic"
  },
  "dependencies": {
    "ioredis": "5.4.1",
    "pino": "8.19.0",
    "prom-client": "15.1.3"
  },
  "devDependencies": {
    "@vitest/coverage-v8": "1.6.0",
    "vitest": "1.6.0"
  }
}
